[build]
cmd = "composer install --no-dev && npm install && npm run build && php artisan config:cache && php artisan route:cache && php artisan view:cache"

[start]
cmd = "php artisan migrate --force && vendor/bin/heroku-php-apache2 public/"
